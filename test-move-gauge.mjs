import assert from "node:assert/strict";
import fs from "node:fs";
const app=fs.readFileSync("app.js","utf8");
const css=fs.readFileSync("styles.css","utf8");
assert.match(app,/function moveOutcomeStats\(node\)/);
assert.match(app,/function moveGaugeMarkup\(stats\)/);
assert.match(app,/moveGaugeMarkup\(moveOutcomeStats\(c\)\)/);
assert.match(app,/Blancs \${w} %, nulles \${d} %, Noirs \${b} %/);
assert.match(css,/\.moveGaugeWrap/);
assert.match(css,/\.moveGaugeLabels/);
console.log("MOVE GAUGE REGRESSION TESTS OK");
